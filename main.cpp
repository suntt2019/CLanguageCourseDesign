#include "zuma.h"

int main(){
	return handleException(0);	
}

int gameMain() {
	//MajorData md;
	//loadMap(&md.mi, "maps","test_points");//TODO：地图选择界面动态决定选哪张图
	//coreGaming(md);
	test();
	return 0;
}